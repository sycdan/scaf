SCAF="$$(cd -- "$$(dirname -- "$${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
DECK="$$(dirname -- "$$SCAF")"
echo "# scaf deck: $$DECK"

# Scaf domain action aliases (added as actions are called):
${aliases_block}
